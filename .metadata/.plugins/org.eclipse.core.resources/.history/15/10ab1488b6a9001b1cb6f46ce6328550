#pragma once
#ifndef __NOVUS_MATH_H
#define __NOVUS_MATH_H

#include <stdint.h>

#define PI 3.14159265

#define LPF_GAIN 79

float map(float target, int from_min, int from_max, int to_min, int to_max);
float getLPF_RPM(float raw_rpm, float pre_rpm, uint32_t loop_time);

#endif
